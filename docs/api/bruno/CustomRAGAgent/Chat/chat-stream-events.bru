meta {
  name: Chat Stream Events
  type: http
  seq: 5
}

post {
  url: {{langgraphBaseUrl}}/threads/{{threadId}}/stream
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "assistant_id": "chat_agent",
    "stream_mode": ["values", "messages", "tools"]
  }
}
