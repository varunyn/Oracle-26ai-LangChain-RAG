# Custom RAG Agent environment configuration
# Copy this file to .env and set your real values there (.env is gitignored).
# Precedence: environment variables > .env > built-in defaults.
# See docs/CONFIGURATION.md for the full reference.

# General / OCI authentication
DEBUG=false
AUTH=API_KEY
OCI_PROFILE=CHICAGO
OCI_CONFIG_FILE=~/.oci/config
REGION=us-chicago-1
COMPARTMENT_ID=ocid1.compartment.oc1..your-compartment-ocid
# Optional; computed from REGION if unset.
# SERVICE_ENDPOINT=

# LLM configuration
LLM_MODEL_ID=meta.llama-3.3-70b-instruct
TEMPERATURE=0.1
MAX_TOKENS=4000
# Optional model picker list; comma-separated or JSON array (single line).
# If unset, defaults are derived from REGION.
# MODEL_LIST=xai.grok-3,xai.grok-4,openai.gpt-4.1,openai.gpt-4o,openai.gpt-5,meta.llama-3.3-70b-instruct,cohere.command-a-03-2025
# Optional UI labels for models; JSON object (single line).
# MODEL_DISPLAY_NAMES={"xai.grok-3":"Grok 3","xai.grok-4":"Grok 4","openai.gpt-4.1":"GPT-4.1","openai.gpt-4o":"GPT-4o","openai.gpt-5":"GPT-5","meta.llama-3.3-70b-instruct":"Llama 3.3 70B","cohere.command-a-03-2025":"Cohere Command A 03 2025"}

# Embeddings configuration
EMBED_MODEL_TYPE=OCI
EMBED_MODEL_ID=cohere.embed-v4.0

# Oracle Vector Store (required for RAG search)
VECTOR_DB_USER=your_user
VECTOR_DB_PWD=your_password
VECTOR_DSN=your_dsn
VECTOR_WALLET_DIR=/path/to/wallet
VECTOR_WALLET_PWD=wallet_password
# CONNECT_ARGS is optional and derived from VECTOR_* values if unset.
# CONNECT_ARGS={"user":"your_user","password":"your_password","dsn":"your_dsn","config_dir":"/path/to/wallet","wallet_location":"/path/to/wallet","wallet_password":"wallet_password"}
DB_TCP_CONNECT_TIMEOUT=5

# RAG / search configuration
RAG_SEARCH_MODE=vector
# Number of documents retrieved before filtering/reranking.
RAG_RETRIEVAL_TOP_K=5
# COLLECTION_LIST can be comma-separated or a JSON array (for example: RAG_KNOWLEDGE_BASE or col1,col2).
COLLECTION_LIST=RAG_KNOWLEDGE_BASE
DEFAULT_COLLECTION=RAG_KNOWLEDGE_BASE
CHUNK_SIZE=4000
CHUNK_OVERLAP=100
DOCLING_DO_OCR=true
DOCLING_FORCE_FULL_PAGE_OCR=false
DOCLING_DOCUMENT_TIMEOUT=90
ENABLE_RERANKER=true
RERANK_MODEL_ID=cohere.rerank-v4.0-fast
# RERANK_DEDICATED_ENDPOINT_ID=
RERANK_TOP_N=5
# RERANK_MAX_CHUNKS_PER_DOCUMENT=
# RERANK_MAX_TOKENS_PER_DOCUMENT=

# UI-facing backend options
ENABLE_USER_FEEDBACK=true
# ENABLE_CORS=true
# CORS_ALLOW_ORIGINS=http://localhost:4000,http://127.0.0.1:4000,http://localhost:3000,http://127.0.0.1:3000

# Conversation memory / threads
# See docs/SERVER-OWNED-MEMORY.md when using these options.
# LANGGRAPH_SQLITE_PATH=.local-data/langgraph-checkpoints.sqlite
# ALLOW_CLIENT_THREAD_ID=true
# THREAD_ID_STRATEGY=uuid4
# THREAD_ID_PREFIX=

# MCP client configuration
ENABLE_MCP_TOOLS=true
# Optional: limit which configured MCP server keys are loaded.
# MCP_SERVER_KEYS=default,context7
MCP_SEARCH_MODE=vector
# Legacy global MCP auth. Prefer per-server auth in Settings.
# ENABLE_MCP_CLIENT_JWT=false
# ENABLE_MCP_OAUTH=false
# MCP_OAUTH_CLIENT_ID=
# MCP_OAUTH_CLIENT_SECRET=
# MCP_OAUTH_TOKEN_URL=
# MCP_OAUTH_SCOPE=
# MCP_OAUTH_GRANT_TYPE=client_credentials
# MCP_OAUTH_AUDIENCE=
# MCP_OAUTH_REFRESH_SKEW_SECONDS=30
# REQUIRE_TOOL_CALL=false
# MCP_REPEATED_WORKFLOW_CONTROLLER=true
# MCP_WORKFLOW_POLICY={}
# MCP_MAX_ROUNDS=4
# Server-side JSON file written by the frontend Settings page after UI edits.
# MCP_UI_CONFIG_FILE=.local-data/mcp_servers.json
# Max seconds the Settings page MCP connection test waits before returning a failure.
# MCP_CONNECTION_TEST_TIMEOUT_SECONDS=8
# OCI_MAX_SEQUENTIAL_TOOL_CALLS=10
# OCI_TOOL_RESULT_GUIDANCE=true
# Optional seed/headless MCP config. The Settings page is the primary way to add MCP servers.
# Leave unset for UI-managed setup. If set, it must be JSON on a single line.
# Example seed when backend and MCP server both run on the host:
# MCP_SERVERS_CONFIG={"calculator":{"transport":"streamable-http","url":"http://localhost:9000/mcp"}}
# Example seed when backend runs in Docker and MCP server runs on the host:
# MCP_SERVERS_CONFIG={"calculator":{"transport":"streamable-http","url":"http://host.docker.internal:9000/mcp"}}
# If the backend runs locally and MCP_SERVERS_CONFIG contains host.docker.internal,
# it is auto-mapped to localhost.
# Supported transports: streamable_http and streamable-http are accepted;
# code mode may map them to http internally.

# MCP server runtime (mcp_servers/*.py)
TRANSPORT=streamable-http
HOST=0.0.0.0
PORT=9000

# Observability / OTEL (optional)
# DOCKER_STACKS is optional; leave unset to auto-include stacks from the flags below.
# See docs/DOCKER-SETUP.md for details.
# DOCKER_STACKS={}
ENABLE_OTEL_TRACING=false
# OTEL_TRACES_ENDPOINT=
# OTEL_TRACES_HEADERS=
# OTEL_LOGS_ENDPOINT=
OTEL_LOGS_BATCH_DELAY_MILLIS=1000
ENABLE_OBSERVABILITY_STACK=false
GRAFANA_URL=http://localhost:3051

# Langfuse tracing (optional)
ENABLE_LANGFUSE_TRACING=false
# Local runs use localhost. In Docker, compose may override this to http://langfuse-web:3000.
LANGFUSE_HOST=http://localhost:3300
# Browser-facing Settings page link. Useful in Docker when LANGFUSE_HOST is an internal service URL.
# LANGFUSE_UI_URL=http://localhost:3300
LANGFUSE_PUBLIC_KEY=pk-lf-your-project-key
LANGFUSE_SECRET_KEY=sk-lf-your-secret-key
LANGFUSE_TRACING_ENVIRONMENT=development
# LANGFUSE_RELEASE=
LANGFUSE_SAMPLE_RATE=0.25

# OCI Logging Analytics (optional)
# ENABLE_OCI_LOGGING_ANALYTICS=false
# LOGGING_ANALYTICS_NAMESPACE=
# LOGGING_ANALYTICS_LOG_GROUP_ID=
# LOGGING_ANALYTICS_LOG_SET=
# LOGGING_ANALYTICS_RESOURCE_CATEGORY=rag-api
# LOGGING_ANALYTICS_META_PROPERTIES=
# LOGGING_ANALYTICS_CONSOLE_URL=
